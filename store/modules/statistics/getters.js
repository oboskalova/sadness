export default {
  selectedStatFile (state) {
    return state.selectedStatFile
  },
  selectedStatDisplays (state) {
    return state.selectedStatDisplays
  },
  selectedStatSchedules (state) {
    return state.selectedStatSchedules
  },
  selectedStatTemplates (state) {
    return state.selectedStatTemplates
  },
  selectedTemplateData (state) {
    return state.selectedTemplateData
  },
  selectedTemplate (state) {
    return state.selectedTemplate
  },
  resizeFunction (state) {
    return state.resizeFunction
  }
}
